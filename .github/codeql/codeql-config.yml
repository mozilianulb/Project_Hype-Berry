name: "Custom CodeQL Config"
disable-default-queries: false
queries:
  - uses: security-extended@latest  # ✅ Use built-in security queries
  - uses: security-and-quality@latest  # ✅ Use built-in security + quality queries
